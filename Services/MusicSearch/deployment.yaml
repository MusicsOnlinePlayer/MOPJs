apiVersion: apps/v1
kind: Deployment
metadata:
  name: mop-musicsearch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: musicsearch
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
      labels:
        app: musicsearch
    spec:
      containers:
      - name: musicsearch
        image: malauryd/mop_musicsearch
        env:
          - name: MONGO_URL
            value: 'mongodb://mongodb-service:27017/MOPv2'
        ports:
        - containerPort: 3000
        resources:
          limits:
           memory: "64Mi"
           cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: musicsearch
  name: musicsearch-service
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: musicsearch
  type: ClusterIP