apiVersion: apps/v1
kind: Deployment
metadata:
  name: mop-usermanager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: usermanager
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
      labels:
        app: usermanager
    spec:
      containers:
      - name: usermanager
        image: malauryd/mop_usermanager
        env:
          - name: MONGO_URL
            value: 'mongodb://mongodb-service:27017/MOPv2'
        ports:
        - containerPort: 3000
        resources:
          limits:
           memory: "64Mi"
           cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: usermanager
  name: usermanager-service
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: usermanager
  type: ClusterIP